import QtQuick 2.0

QtObject {
    // 主要背景色
    property color backgroundColor: "#2d2d2d"
    // 次要背景色
    property color secondaryBackgroundColor: "#3d3d3d"
    // 主要文本颜色
    property color textColor: "#ffffff"
    // 次要文本颜色
    property color secondaryTextColor: "#aaaaaa"
    // 边框颜色
    property color borderColor: "#555555"
    // 按钮背景色
    property color buttonBackgroundColor: "#444444"
    // 按钮文本颜色
    property color buttonTextColor: "#ffffff"
    // 按钮悬停背景色
    property color buttonHoverBackgroundColor: "#555555"
    // 强调色
    property color accentColor: "#0099ff"
    // 游戏板背景色
    property color gameBoardBackgroundColor: "#333333"
    // 方块边框颜色
    property color blockBorderColor: "#666666"
    // 选中方块边框颜色
    property color selectedBlockBorderColor: "#0099ff"
    // 连接线颜色
    property color connectionLineColor: "#0099ff"
}
