import QtQuick 2.0

QtObject {
    // 主要背景色
    property color backgroundColor: "#f0f0f0"
    // 次要背景色
    property color secondaryBackgroundColor: "#f8f8f8"
    // 主要文本颜色
    property color textColor: "#333333"
    // 次要文本颜色
    property color secondaryTextColor: "#666666"
    // 边框颜色
    property color borderColor: "#dddddd"
    // 按钮背景色
    property color buttonBackgroundColor: "#e0e0e0"
    // 按钮文本颜色
    property color buttonTextColor: "#333333"
    // 按钮悬停背景色
    property color buttonHoverBackgroundColor: "#d0d0d0"
    // 强调色
    property color accentColor: "#0078d7"
    // 游戏板背景色
    property color gameBoardBackgroundColor: "#ffffff"
    // 方块边框颜色
    property color blockBorderColor: "#aaaaaa"
    // 选中方块边框颜色
    property color selectedBlockBorderColor: "#0078d7"
    // 连接线颜色
    property color connectionLineColor: "#0078d7"
}
